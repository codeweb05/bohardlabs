/** Wire format for date filter values. ISO so a server can parse it without a locale. */
export const DATE_API_FORMAT = 'YYYY-MM-DD';

/** What the date filter's picker shows. Override per consumer if the locale differs. */
export const DATE_PICKER_FORMAT = 'DD/MM/YYYY';
